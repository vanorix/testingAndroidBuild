on:
  push:
   branches:
    - 'main'
    - 'master'
jobs:
  build:
    name: Build
    runs-on: macos-latest
    steps:
     - name: Android Build
       uses: vgaidarji/android-github-actions-build@v1.0.1
       with:
          args: "./gradlew app:assembleRelease"

on:
  push:
   branches:
    - 'main'
    - 'master'
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
     - name: Android Build
       uses: vgaidarji/android-github-actions-build@v1.0.1
       with:
          args: "./cicdpipelinetest/android/gradlew app:assembleRelease"
